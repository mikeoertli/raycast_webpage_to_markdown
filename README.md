# Raycast Webpage to Markdown Local

The Raycast extension source lives in [`webpage-to-markdown-local`](./webpage-to-markdown-local). It converts webpages to Markdown locally, without a cloud conversion service.
